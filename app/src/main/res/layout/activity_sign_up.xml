<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@drawable/gradient">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="120dp"
        android:layout_height="150dp"
        android:layout_marginTop="40dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        app:srcCompat="@drawable/exercisebuddy" />

    <EditText
        android:id="@+id/emailReg"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_below="@+id/imageView"
        android:background="#11000000"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:inputType="textEmailAddress"
        android:textSize="16dp"
        android:hint="Email"/>

    <EditText
        android:id="@+id/passwordReg"
        android:layout_width="250dp"
        android:layout_height="40dp"
        android:layout_alignStart="@+id/emailReg"
        android:layout_below="@+id/emailReg"
        android:layout_marginTop="16dp"
        android:background="#11000000"
        android:hint="Password"
        android:inputType="textPassword"
        android:textSize="16dp" />

    <Button
        android:id="@+id/registerToFirebase"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/passwordReg"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="29dp"
        android:text="Register!!!" />

    <Button
        android:id="@+id/cancel"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/registerToFirebase"
        android:layout_alignLeft="@+id/registerToFirebase"
        android:layout_alignRight="@+id/registerToFirebase"
        android:layout_below="@+id/registerToFirebase"
        android:text="Cancel" />


</RelativeLayout>
